# @defaultRequired=true @defaultSensitive=false
# @generateTypes(lang=ts, path=env.d.ts)
# @import(./.env.build.schema)
# @import(./.env.runtime.schema, enabled=not($DOCKER_BUILD))
# ---

# @type=boolean
DOCKER_BUILD=false
